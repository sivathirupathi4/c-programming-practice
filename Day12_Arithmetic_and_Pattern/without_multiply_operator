#include <stdio.h>
int main()
{
    int n,m;
    printf("Enter the two numbers : ");
    scanf("%d %d",&n, &m);

    int add=0;
    for(int i=1;i<=m;i++)
    {
        add=add+n;
    }
    printf("The Result is %d",add);
}
