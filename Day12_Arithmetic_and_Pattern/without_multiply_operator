#include <stdio.h>
int main(){
    int n,m,add=0;
    printf("Enter the two numbers : ");
    scanf("%d %d",&n, &m);
    
    for(int i=1;i<=m;i++){
        add=add+n;
    }
    printf("The Result is %d",add);
}
